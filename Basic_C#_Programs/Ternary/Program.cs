using System;

class Program
{
    static void Main()
    {
        int number = 10;

        string output = (number > 5) ? "Number is greater than 5" : "Number is less than or equal to 5";

        Console.WriteLine(output);
    }
}
